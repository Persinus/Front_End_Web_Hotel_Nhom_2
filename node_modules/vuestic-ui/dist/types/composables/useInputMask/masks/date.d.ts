import { Mask, MaskToken } from 'vuestic-ui/dist/types/composables/useInputMask/mask.js';
type MinorToken = {
    value: string;
    expect: string;
    static: boolean;
};
export declare const createDateMask: (format?: string) => Mask<MaskToken, MinorToken[]>;
export {};
